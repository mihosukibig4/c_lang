#include <stdio.h>

/* roop  */

int main(void){

    int m = 0;
    while (m<11){
      printf("m: %d\n", m);
      m++;

    }

		int n = 0;
		do{
			printf("n: %d\n", n);
			n++;
		} while (n < 11);
		
    return 0;
}
